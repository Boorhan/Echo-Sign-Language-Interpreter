<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.echo">

    <uses-sdk />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignToText" />
        <activity android:name=".FrequentlyUsed" />
        <activity android:name=".Number" />
        <activity android:name=".Alphabet" />
        <activity android:name=".LearnMenu" />
        <activity android:name=".SpeechToSign" />
        <activity android:name=".SpeechToText" />
        <activity android:name=".TextToSign" />
        <activity android:name=".TextToSpeech" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>